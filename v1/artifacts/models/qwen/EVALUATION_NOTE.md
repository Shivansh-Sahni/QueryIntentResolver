# Qwen V1 evaluation

The committed raw export was scored exactly as produced by the unchanged inference pipeline. `predicted_route_raw` was treated as the model's end-to-end route output. No post-hoc intent reparsing, fuzzy intent correction, generation-parameter change, or benchmark-error tuning was applied.
